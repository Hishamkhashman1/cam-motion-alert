import os
import time
from datetime import datetime
from pathlib import Path

import cv2
from dotenv import load_dotenv
from twilio.rest import Client


# Define variables (what URL for RTSP with username and password in .env, TWILIO account for whatsapp notifications , account, toke, whatsapp from , whatsapp to, and SNAPshot URL for sending the snapshot of movement with notification.

# Define control area, alert cooldown and frame wedth


# raise errors if RTSP link not present 

# raise errors if TWILIO is not funcitonal

# define send whatsapp alert funciton

# define save snapshot funciton

# define build snapshot function

# define preprocess

# define main funciton 

